import { Input } from 'antd';
import React from 'react';

const DogPage = () => (
  <div>
    Page of Dog. The content of the input should be kept when switching tabs
    <Input />
  </div>
)

export default DogPage;